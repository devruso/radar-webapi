package com.jangada.RADAR.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfig {
    // DataSource é configurado automaticamente pelo Spring Boot via application.properties
    // Remover esta classe é recomendado para não sobrescrever a autoconfiguration
}

